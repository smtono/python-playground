"""
This module is used to create templates based on a given set of text
Keywords are found and replaced with variables to be replaced in a given template
Templates are given a general name, list of variables, 
and the template text that will be used to safe substitue variables later on
"""

class TemplateBuilder:
    """used to create generic templates"""
    
    def __init__(self, name: str, var_names: list[str], text: str) -> None:
        """initializes the class with template text to be rendered"""
    
    def find_variables(self):
        """finds the variables to be created within the template"""
        
    
